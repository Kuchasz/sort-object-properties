const _sortedObject = {
    a: 'value',
    g: 'value',
    j: 'value',
    kolm: 'value',
    zet: 'value'
};

const _unsortedObject = {
    kolm: 'value',
    g: 'value',
    zet: 'value',
    j: 'value',
    a: 'value'
};

describe('sort-object-properties', () => {
    test('object properties sorted properly',()=>{
        expect(2).toEqual(2);
    });
});